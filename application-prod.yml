spring:
 application:
  name: Config-Dev
eureka:
 client:
  fetch-registry: true
  registry-with-eureka: true
  service-url:
   default-zone: http://localhost:8761/eureka
 instance:
  prefer-ip-instance: true
